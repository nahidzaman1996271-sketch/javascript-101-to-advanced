// arithmetic opertaors = operands (values, variables, etc)
//                        operators (- / * +)
//                        ex. 11 = x + 5;

// let students = 30;

// students = students + 1;
// students = students - 1;
// students = students * 2;
// students = students / 2;
// students = students ** 2;
// students = students % 2;
// let extraStudents = students % 3;

// These are the shortcuts of the above funtions

// students += 1;
// students -= 1;
// students /= 1;
// students *= 1;
// students %= 1;

// students++;
// students--;

/*
    operator precedence
    1. parenthesis ()
    2. exponents
    3. multiplication & division & modulo
    4. addition & substraction
*/

let result = 1 + 2 * 3 + 4 ** 2;

console.log(result);